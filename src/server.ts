export const startServer = (): void => {
  // Intentionally minimal entry point for starter kit users.
  // Replace this with real app bootstrap logic.
  console.log('Server bootstrap placeholder');
};

if (require.main === module) {
  startServer();
}
